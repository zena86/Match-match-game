export default class CardDifficulty {
  public difficultyName: string;

  public difficultySize: number;

  constructor(difficultyName: string, difficultySize: number) {
    this.difficultyName = difficultyName;
    this.difficultySize = difficultySize;
  }
}
