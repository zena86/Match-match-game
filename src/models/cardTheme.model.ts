export default class CardTheme {
  public themeName: string;

  public themeCardNum: number;

  constructor(themeName: string, themeCardNum: number) {
    this.themeName = themeName;
    this.themeCardNum = themeCardNum;
  }
}
